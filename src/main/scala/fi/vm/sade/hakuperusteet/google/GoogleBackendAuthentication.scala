package fi.vm.sade.hakuperusteet.google

import com.typesafe.config.Config

object GoogleBackendAuthentication {
  def authenticate(config: Config, email: String, token: String) = {
    //todo: authenticate google
    true
  }
}
